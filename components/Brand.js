import { Text } from "react-native";
import AppStyles from "../styles/AppStyles";

export default function InLineTextButton(props) {
    return (
        <Text style={AppStyles.brand}>ToDo</Text>
    )
}